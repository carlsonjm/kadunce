# Accepted Bento sizing and full-divider freeze

J accepted the installed build and authorized freeze/publication September14.
SHA256:302b1cedf851c4015f171311cd7e6e5cfc44a2d042d16923af9c34bbdf00b907.
Preceding local membership/overflow freeze0184129 is included in this push.

Accepted: output-local membership/minimize reflow, side/share placement,
minimum-aware overflow with required visible arrivals, occupied-column splits,
and both divider axes. Full-length32px cross-axis hit targets capture at down;
90ms hold reveals pill/preview, release applies once. Idle rails hide. No global
Plasma desktop-edit lock. Native carry contact handoff clears forwarded ownership.

Validation: production build, focused layout/input/transfer tests, verify-control;
private NYkGoz/l96RD3 off-center grabs, early movement, split footprint, overflow,
cancel and held-unload checks. Installed hash/live safety switch verified at freeze.
J owns physical acceptance and reported a solid pass. Monitor testing deferred.

Local installer ../install-kadunce-bento-fullrail-20260914.sh; --rollback restores
cf5011ede1b72da74d754347726c06b7a8de45357524990746e5a4879195b35f.
Older accepted rail/overflow bundles remain local, outside Git. Fresh clones use install.sh.

## Remaining scoped slices

1. Deferred monitor ownership/handoff and large-layout physical acceptance; fix only
   reproduced divergence, not a speculative rebuild of the accepted tablet path.
2. Bento displaced-neighbor/reflow motion; existing line/stack/pickup motion is passed.
3. Zen sampling artifact, separately bounded; Affinity splash remains nonblocking.
4. Consumer release checkpoint: fresh install/update/uninstall/recovery, gesture
   documentation and supported hardware acceptance. Not a new architecture block.

Tette All Files/Active drawers and subsequent Ambient work remain separate product
milestones. Temperance power/notification-handback items require rechecking current
source before labeling them open defects. Preserve existing uncommitted suite-roadmap
work; it is not part of this scoped compositor freeze.
