# September 12 development checkpoint

User approved freezing and pushing the accumulated Kadunce overhaul after the
three native-return tests passed. This is a source checkpoint, not an all-green
production release or permission to promote trusted repair.

## Included

- Controller-owned card state, guarded restoration and transactional transfer.
- Native pointer/touch carry with Wayland and Xwayland pickup correlation.
- Deferred ordinary-window ownership until workspace/edge entry.
- Tablet/monitor admission, Bento previews, pane exchange and bottom departure.
- Cross-output ordinary landing correction and work-area safety clearance.
- Regression probes, architecture decisions, recovery provenance and handoff docs.

Installed/tested binary SHA256:
`f30825c0514f7f72129ededa48f2b5e54a2d31cb764ae971724419d5c01aa57b`.
Production source matches the frozen integrated-gate source exactly.
14 CTests, 15 private runtime routes, Bento restoration/unload, source guards and
independent control checks passed; extra Xwayland tablet pickup also passed.
See NATIVE-RETURN-TEST.md for evidence and recovery bundle references.

## Physical acceptance

1. Tablet Active window → free monitor workspace stays a normal window.
2. Ordinary monitor window → tablet transit → monitor stays ordinary.
3. Basic side-edge Bento and bottom-edge departure pass.

## Explicit open issues

- Bottom-exited ordinary window cannot always re-enter existing Bento; native
  KDE edge snapping appears over the layout. Cause not diagnosed. Test the full
  exit → new drag → edge sequence; do not treat this as accepted re-entry.
- Occupied-tablet edge admission contention and new-launch admission remain
  roadmap work; the broad overhaul is not declared complete.
- Stack/motion convergence and companion-package improvements remain queued.

No installed files, session state, or trusted repair archive are changed by this
freeze. Machine-local candidate/rollback binaries and temporary compositor logs
are not repository artifacts. CURRENT_STATE.md is the restart index;
NEXT-ROADMAP.md and HEAVY-REMAINING.md define remaining work.
